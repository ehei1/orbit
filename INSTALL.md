Install

windows only

```bash
# it needs long times
#
# you can have some error... problem: self signed certificate in certificate chain. 
# When it occurs, you may execute it in shell
#
# git config --global http.sslVerify false
.\vcpkg\vcpkg install --feature-flags=manifests


```
